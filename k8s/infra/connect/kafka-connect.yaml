# Author: Nicholas Irvine  GitHub https://github.com/SaladStik  LinkedIn https://www.linkedin.com/in/nicholas-irvine-303ab5284/
# Strimzi Connect cluster hosting the Debezium connector. image has the plugin baked in.
apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaConnect
metadata:
  name: orbit-connect
  namespace: orbit
  annotations:
    strimzi.io/use-connector-resources: "true"
spec:
  version: 3.8.0
  replicas: 1
  bootstrapServers: orbit-kafka-kafka-bootstrap:9092
  image: ghcr.io/your-org/orbit-debezium-connect:latest
  config:
    group.id: orbit-connect
    offset.storage.topic: connect-offsets
    config.storage.topic: connect-configs
    status.storage.topic: connect-status
    config.storage.replication.factor: 3
    offset.storage.replication.factor: 3
    status.storage.replication.factor: 3
    key.converter: org.apache.kafka.connect.json.JsonConverter
    value.converter: org.apache.kafka.connect.json.JsonConverter
    key.converter.schemas.enable: false
    value.converter.schemas.enable: false
